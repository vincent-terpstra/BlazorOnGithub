function getItem(key) {
    return localStorage.getItem(key);
}

function setItem(key, value){
    localStorage.setItem(key, value);
}
// added this to confirm when the script loads
// alert("Scripts Loaded")